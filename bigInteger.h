#include <stdio.h>

int min(int nb1, int nb2);
int max(int nb1, int nb2);
char* addition_base(char *n1, char *n2);
char* inverseTableau(char *tableau);
char* addition(char* n1, char* n2);
char* soustraction(char* n1, char* n2);
char* multiplication(char* n1, char* n2);

typedef char* bigInt;